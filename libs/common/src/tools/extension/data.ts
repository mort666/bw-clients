export const Site = Object.freeze({
  forwarder: "forwarder",
} as const);

export const Field = Object.freeze({
  token: "token",
  baseUrl: "baseUrl",
  domain: "domain",
  prefix: "prefix",
} as const);
